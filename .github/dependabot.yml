version: 2
updates:
  # npm dependencies — security patches and version bumps.
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5

  # Keep the GitHub Actions used in the deploy workflow patched too.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
