# Skyland Ranch currently requires no environment variables.
#
# The Lovable branch is a TanStack Start + React marketing site with no database,
# forms, or server-side secrets. Add variables here only if a future feature
# requires runtime configuration.
